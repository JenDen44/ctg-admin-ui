name: Codex Comment-Triggered PR-wide Review

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  review:
    # Любой новый комментарий внутри PR, содержащий '/codex'
    if: >
      github.event.issue.pull_request != null &&
      contains(github.event.comment.body, '/codex')
    runs-on: ubuntu-latest

    env:
      OPENAI_MODEL: gpt-4.1-mini

    steps:
      - name: Контекст
        run: |
          echo "PR_NUMBER=${{ github.event.issue.number }}" >> $GITHUB_ENV
          echo "COMMENT_URL=${{ github.event.comment.html_url }}" >> $GITHUB_ENV

      - name: Проверка прав комментатора (опционально)
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const assoc = context.payload.comment.author_association;
            const allowed = ['OWNER','MEMBER','COLLABORATOR'];
            if (!allowed.includes(assoc)) {
              core.info(`Пропуск: уровень доступа = ${assoc}`);
              core.setOutput('skip', 'true');
            }

      - name: Остановить если нет прав
        if: steps.gate-commenter.outputs.skip == 'true'
        run: echo "Нет прав для запуска" && exit 0

      - name: Получение полного diff PR
        id: globaldiff
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR=$PR_NUMBER
          curl -sSL \
            -H "Accept: application/vnd.github.v3.diff" \
            -H "Authorization: Bearer $GH_TOKEN" \
            "${{ github.api_url }}/repos/${{ github.repository }}/pulls/${PR}" > global.diff

          MAX=350000
          if [ $(wc -c < global.diff) -gt $MAX ]; then
            head -c $MAX global.diff > global_trimmed.diff
            mv global_trimmed.diff global.diff
            echo "TRUNCATED=true" >> $GITHUB_ENV
          else
            echo "TRUNCATED=false" >> $GITHUB_ENV
          fi

      - name: Получение патчей по файлам (для inline-комментов)
        id: files
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          PR=$PR_NUMBER
          API="${{ github.api_url }}/repos/${{ github.repository }}/pulls/${PR}/files?per_page=100&page="
          page=1
          echo "[]" > files.json
          while :; do
            RESP=$(curl -sSL -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "${API}${page}")
            COUNT=$(echo "$RESP" | jq 'length')
            jq -s 'add' files.json <(echo "$RESP") > tmp.json && mv tmp.json files.json
            [ "$COUNT" -lt 100 ] && break
            page=$((page+1))
          done
          jq '[ .[] | {path: .filename, status: .status, patch: .patch} | select(.patch != null) ]' files.json > patches.json
          echo "Получено $(jq 'length' patches.json) текстовых патчей"

      - name: Генерация AI-предложений (inline + общие)
        id: ai
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ env.OPENAI_MODEL }}
        run: |
          GLOBAL=$(jq -Rs . < global.diff)

          SYS_PROMPT=$(cat << 'EOF'
          Ты — сеньор Frontend-инженер (React + TypeScript + React Query). Тебе дан unified diff ВСЕГО PR.
          ВАЖНО: отвечай ТОЛЬКО на русском языке. Все поля "message" в JSON должны быть на русском.

          Нужно выдать:
          1) Точечные ИНЛАЙН-комментарии к добавленным строкам (если можешь точно привязать).
          2) Общие замечания по PR (кросс-файловые, архитектура, CI, тесты и пр.).

          Формат ОТВЕТА строго JSON:
          {
            "inline": [
              {
                "path": "relative/path/to/file.tsx",
                "match": "<ТОЧНОЕ содержимое одной ДОБАВЛЕННОЙ строки без ведущего '+'>",
                "message": "<краткий, предметный комментарий (до 2-3 предложений, по-русски)>",
                "severity": "nit|suggestion|warn|error"
              }
            ],
            "general": [
              {
                "message": "<общее замечание/риск/рекомендация, по-русски>",
                "severity": "nit|suggestion|warn|error"
              }
            ]
          }
          EOF
          )

          REQ=$(jq -n \
            --arg model "$OPENAI_MODEL" \
            --arg sys "$SYS_PROMPT" \
            --arg content "PR unified diff:\n\n$GLOBAL" \
            '{model:$model, temperature:0.2, messages:[{role:"system",content:$sys},{role:"user",content:$content}] }')

          RESP=$(curl -sS https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer '"$OPENAI_API_KEY"'" \
            -d "$REQ" | jq -r '.choices[0].message.content')

          if echo "$RESP" | jq type >/dev/null 2>&1; then
            echo "$RESP" > suggestions.json
          else
            echo '{"inline":[],"general":[{"message":"Модель вернула невалидный JSON.","severity":"warn"}]}' > suggestions.json
          fi

      - name: Создание ревью в GitHub (inline) + общий комментарий
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            const prNumber = Number(process.env.PR_NUMBER);
            const truncated = process.env.TRUNCATED === 'true';

            const sug = JSON.parse(fs.readFileSync('suggestions.json','utf8'));
            const patches = JSON.parse(fs.readFileSync('patches.json','utf8'));

            const patchMap = new Map();
            for (const f of patches) {
              const lines = (f.patch || '').split('\n');
              patchMap.set(f.path, lines);
            }

            const findPosition = (lines, wanted) => {
              const want = String(wanted || '').trim();
              for (let i=0;i<lines.length;i++) {
                const line = lines[i];
                if (!line.startsWith('+')) continue;
                if (line.startsWith('+++')) continue;
                const added = line.slice(1).trim();
                if (added === want) return i+1;
              }
              return null;
            };

            const inline = Array.isArray(sug.inline) ? sug.inline : [];
            const general = Array.isArray(sug.general) ? sug.general : [];

            const sevEmoji = s => ({nit:'🧹',suggestion:'💡',warn:'⚠️',error:'🚨'})[String(s||'').toLowerCase()] || '💬';

            const comments = [];
            for (const it of inline) {
              const path = it.path;
              const lines = patchMap.get(path);
              if (!lines) continue;
              const pos = findPosition(lines, it.match);
              if (!pos) continue;
              const body = `${sevEmoji(it.severity)} ${it.message || 'Проверьте эту строку.'}`;
              comments.push({ path, position: pos, body });
            }

            if (comments.length > 0) {
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
                event: 'COMMENT',
                body: `🤖 Codex инлайн-ревью • ${comments.length} комментариев\n_Запущено из ${process.env.COMMENT_URL}_`,
                comments
              });
            }

            let generalBody = '';
            if (general.length > 0) {
              generalBody += `### 🤖 Codex обзор PR (общие замечания)\n`;
              for (const g of general) {
                generalBody += `- ${sevEmoji(g.severity)} ${g.message}\n`;
              }
              if (truncated) {
                generalBody += `\n> ⚠️ Дифф был усечён по размеру для анализа.\n`;
              }
              generalBody += `\n_Запущено из ${process.env.COMMENT_URL}_`;
            } else if (comments.length === 0) {
              generalBody = `🤖 Codex: замечаний нет.\n_Запущено из ${process.env.COMMENT_URL}_`;
            }

            if (generalBody) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: generalBody
              });
            }

      - name: Завершено
        run: echo "Ревью опубликовано для PR #$PR_NUMBER"
