name: Codex Comment-Triggered PR-wide Review

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  review:
    if: >
      github.event.issue.pull_request != null &&
      contains(github.event.comment.body, '/review')
    runs-on: ubuntu-latest

    env:
      OPENAI_MODEL: gpt-4.1-mini

    steps:
      - name: Контекст
        run: |
          echo "PR_NUMBER=${{ github.event.issue.number }}" >> $GITHUB_ENV
          echo "COMMENT_URL=${{ github.event.comment.html_url }}" >> $GITHUB_ENV

      - name: Сообщение о старте ревью
        uses: actions/github-script@v7
        with:
          script: |
            const body = [
              "🤖 Codex: начал ревью этого PR.",
              "",
              `_Запущено из ${process.env.COMMENT_URL}_`
            ].join("\n");
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: Number(process.env.PR_NUMBER),
              body
            });

      - name: Получение полного diff PR
        id: globaldiff
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          PR=$PR_NUMBER
          curl -sSL \
            -H "Accept: application/vnd.github.v3.diff" \
            -H "Authorization: Bearer $GH_TOKEN" \
            "${{ github.api_url }}/repos/${{ github.repository }}/pulls/${PR}" > global.diff

          MAX=350000
          if [ $(wc -c < global.diff) -gt $MAX ]; then
            head -c $MAX global.diff > global_trimmed.diff
            mv global_trimmed.diff global.diff
            echo "TRUNCATED=true" >> $GITHUB_ENV
          else
            echo "TRUNCATED=false" >> $GITHUB_ENV
          fi

      - name: Получение патчей по файлам (для inline-комментов)
        id: files
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          PR=$PR_NUMBER
          API="${{ github.api_url }}/repos/${{ github.repository }}/pulls/${PR}/files?per_page=100&page="
          page=1
          echo "[]" > files.json
          while :; do
            RESP=$(curl -sSL -H "Authorization: Bearer $GH_TOKEN" -H "Accept: application/vnd.github+json" "${API}${page}")
            COUNT=$(echo "$RESP" | jq 'length')
            jq -s 'add' files.json <(echo "$RESP") > tmp.json && mv tmp.json files.json
            [ "$COUNT" -lt 100 ] && break
            page=$((page+1))
          done
          jq '[ .[] | {path: .filename, status: .status, patch: .patch} | select(.patch != null) ]' files.json > patches.json
          echo "Получено $(jq 'length' patches.json) текстовых патчей"

      - name: Генерация AI-предложений (inline + общие)
        id: ai
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ env.OPENAI_MODEL }}
        run: |
          set -euo pipefail

          GLOBAL=$(jq -Rs . < global.diff)

          SYS_PROMPT=$(cat << 'EOF'
          Ты — сеньор Frontend-инженер (React + TypeScript + React Query). ВАЖНО: отвечай ТОЛЬКО на русском.

          Верни СТРОГО JSON:
          {
            "inline": [
              {"path":"...", "match":"<добавленная_строка>", "message":"...", "severity":"nit|suggestion|warn|error"}
            ],
            "general": [
              {"message":"...", "severity":"nit|suggestion|warn|error"}
            ]
          }
          EOF
          )

          REQ=$(jq -n \
            --arg model "$OPENAI_MODEL" \
            --arg sys "$SYS_PROMPT" \
            --arg content "PR unified diff:\n\n$GLOBAL" \
            '{model:$model, temperature:0.2, messages:[{role:"system",content:$sys},{role:"user",content:$content}] }')

          RAW=$(curl -sS https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer '"$OPENAI_API_KEY"'" \
            -d "$REQ" | jq -r '.choices[0].message.content // ""')

          # Лог: сохраняем сырой ответ модели
          printf "%s" "$RAW" > raw_model_response.txt

          # Снимаем возможные кодовые блоки ```json ... ```
          CLEAN=$(printf "%s" "$RAW" | sed -E '1,/^```[a-zA-Z0-9]*$/d; /^```$/,$d' || true)
          if [ -z "$CLEAN" ]; then CLEAN="$RAW"; fi

          VALID=false
          if echo "$CLEAN" | jq -e 'type=="object"' >/dev/null 2>&1; then
            VALID=true
            echo "$CLEAN" > suggestions.json
          else
            echo '{"inline":[],"general":[{"message":"Модель вернула невалидный JSON или null.","severity":"warn"}]}' > suggestions.json
          fi

          # Нормализация: гарантируем массивы
          jq '{
                inline: ( .inline // [] | arrays ),
                general:( .general // [] | arrays )
              }' suggestions.json > suggestions.normalized.json
          mv suggestions.normalized.json suggestions.json

          # Экспортируем флаги/пути в outputs шага
          echo "valid=$VALID" >> "$GITHUB_OUTPUT"
          echo "raw_file=raw_model_response.txt" >> "$GITHUB_OUTPUT"

      - name: Диагностика ответа модели (если невалиден)
        if: steps.ai.outputs.valid != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const prNumber = Number(process.env.PR_NUMBER);
            let raw = '';
            try { raw = fs.readFileSync('raw_model_response.txt','utf8'); } catch {}
            const preview = (raw || '').slice(0, 800);
            const body = [
              "⚠️ Codex: ответ модели оказался невалидным JSON, использую дефолтную заготовку без инлайн-комментариев.",
              "",
              "Короткий лог (обрезано до 800 символов):",
              "",
              "```",
              preview,
              "```",
              "",
              "_Запущено из " + process.env.COMMENT_URL + "_"
            ].join("\n");
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body
            });

      - name: Debug suggestions.json
        run: |
          echo "---- suggestions.json (exists?) ----"
          ls -l suggestions.json || true
          echo "---- suggestions.json (first 400 chars) ----"
          head -c 400 suggestions.json || true
          echo
          echo "---- jq type ----"
          (jq -r 'type' suggestions.json 2>/dev/null) || echo "jq failed"

      - name: Sanitize suggestions.json
        run: |
          set -euo pipefail
          # если файла нет — создаём дефолт
          if [ ! -f suggestions.json ]; then
            echo '{"inline":[],"general":[]}' > suggestions.json
          fi

          RAW="$(cat suggestions.json || true)"

          # Пустой файл или одни пробелы
          if [ -z "${RAW// }" ]; then
            echo '{"inline":[],"general":[]}' > suggestions.json
          else
            # Если это именно JSON-литерал null — заменяем
            if echo "$RAW" | jq -e 'type=="null"' >/dev/null 2>&1; then
              echo '{"inline":[],"general":[]}' > suggestions.json
            # Если это не объект — тоже заменяем
            elif ! echo "$RAW" | jq -e 'type=="object"' >/dev/null 2>&1; then
              echo '{"inline":[],"general":[]}' > suggestions.json
            else
              # Обеспечим поля inline/general как массивы
              echo "$RAW" | jq '{
                inline: ( .inline // [] | (if type=="array" then . else [] end) ),
                general: ( .general // [] | (if type=="array" then . else [] end) )
              }' > suggestions.json.tmp
              mv suggestions.json.tmp suggestions.json
            fi
          fi

          echo "== suggestions.json (sanitized) =="
          cat suggestions.json

      - name: Создание ревью в GitHub (inline) + общий комментарий
        id: publish
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');

            // Примитивный надёжный парс: {} при любой проблеме
            function readJsonObject(path) {
              try {
                if (!fs.existsSync(path)) return {};
                const raw = String(fs.readFileSync(path, 'utf8') || '').trim();
                if (!raw) return {};
                const data = JSON.parse(raw);
                return (data && typeof data === 'object' && !Array.isArray(data)) ? data : {};
              } catch (e) {
                return {};
              }
            }

            const prNumber = Number(process.env.PR_NUMBER);
            const truncated = process.env.TRUNCATED === 'true';

            // === Suggestions ===
            const sug = readJsonObject('suggestions.json');
            const inline = Array.isArray(sug && sug.inline) ? sug.inline : [];
            const general = Array.isArray(sug && sug.general) ? sug.general : [];

            // === Patches ===
            let patches = [];
            try {
              const rawP = String(fs.readFileSync('patches.json','utf8') || '').trim();
              const p = rawP ? JSON.parse(rawP) : [];
              patches = Array.isArray(p) ? p : [];
            } catch (e) {
              patches = [];
            }

            const patchMap = new Map();
            for (const f of patches) {
              const path = f && f.path;
              const patch = f && f.patch;
              if (!path || typeof patch !== 'string') continue;
              patchMap.set(path, patch.split('\n'));
            }

            function findPosition(lines, wanted) {
              if (!Array.isArray(lines) || !wanted) return null;
              const want = String(wanted).trim();
              for (let i = 0; i < lines.length; i++) {
                const line = lines[i] || '';
                if (!line.startsWith('+')) continue;
                if (line.startsWith('+++')) continue;
                const added = line.slice(1).trim();
                if (added === want) return i + 1; // position начинается с 1
              }
              return null;
            }

            const sevEmoji = s => ({nit:'🧹',suggestion:'💡',warn:'⚠️',error:'🚨'})[String(s||'').toLowerCase()] || '💬';

            const comments = [];
            for (const it of inline) {
              if (!it || typeof it !== 'object') continue;
              const path = it.path;
              const match = it.match;
              if (!path || typeof match !== 'string') continue;
              const lines = patchMap.get(path);
              if (!lines) continue;
              const pos = findPosition(lines, match);
              if (!pos) continue;
              const body = `${sevEmoji(it.severity)} ${it.message || 'Проверьте эту строку.'}`;
              comments.push({ path, position: pos, body });
            }

            if (comments.length > 0) {
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: prNumber,
                event: 'COMMENT',
                body: `🤖 Codex инлайн-ревью • ${comments.length} комментариев\n_Запущено из ${process.env.COMMENT_URL}_`,
                comments
              });
            }

            let generalBody = '';
            if (general.length > 0) {
              generalBody += `### 🤖 Codex обзор PR (общие замечания)\n`;
              for (const g of general) {
                if (!g || typeof g !== 'object') continue;
                generalBody += `- ${sevEmoji(g.severity)} ${g.message}\n`;
              }
              if (truncated) {
                generalBody += `\n> ⚠️ Дифф был усечён по размеру для анализа.\n`;
              }
              generalBody += `\n_Запущено из ${process.env.COMMENT_URL}_`;
            } else if (comments.length === 0) {
              generalBody = `🤖 Codex: замечаний нет.\n_Запущено из ${process.env.COMMENT_URL}_`;
            }

            if (generalBody) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: generalBody
              });
            }

            core.setOutput('inline_count', String(comments.length));
            core.setOutput('general_count', String(general.length));
            core.setOutput('truncated', truncated ? 'true' : 'false');


      - name: Завершено
        run: echo "Ревью опубликовано для PR #$PR_NUMBER"

      - name: Сообщение о завершении ревью (с счётчиком + пинг автора)
        uses: actions/github-script@v7
        with:
          script: |
            const inlineCount = Number('${{ steps.publish.outputs.inline_count || 0 }}');
            const generalCount = Number('${{ steps.publish.outputs.general_count || 0 }}');
            const truncated = '${{ steps.publish.outputs.truncated || 'false' }}' === 'true';
            const actor = process.env.GITHUB_ACTOR || '${{ github.actor }}';

            const parts = [
              `🤖 Codex: ревью завершено ✅`,
              ``,
              `Итог: ${inlineCount} инлайн, ${generalCount} общих замечаний.` + (truncated ? `\n> ⚠️ Дифф был усечён для анализа.` : ''),
              ``,
              `Запустил: @${actor}`,
              ``,
              `_Запущено из ${process.env.COMMENT_URL}_`
            ];

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: Number(process.env.PR_NUMBER),
              body: parts.join("\n")
            });
